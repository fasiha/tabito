---
import { getSentenceFromPlain } from "../../db";
import { SentenceEditor } from "../../components/SentenceEditor";

const { plain } = Astro.params;
if (!plain) throw new Error("plain not provided unexpectedly");

// const sentence = getSentenceFromPlain(plain);
// if (!sentence) throw new Error("sentence unexpectedly not found in database");
---

<SentenceEditor plain={plain} client:only />
<!-- <SentenceEditor sentence={sentence} client:only /> -->
